<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fot_daily"
        tools:targetApi="31">
        <!-- SplashScreenActivity set as launcher -->
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity no longer launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true" >


        </activity>
        <activity android:name=".SignUpActivity">
        </activity>
        <activity android:name=".LoginActivity" android:exported="true">
            <!-- No intent filter for LoginActivity -->
        </activity>
        <activity
            android:name=".ViewNewsActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".NewsDetailActivity"
            android:exported="false">
        </activity>
        <activity
            android:name=".ViewAcademicNewsActivity"
            android:exported="false"/>

        <activity
            android:name=".ViewSportsNewsActivity"
            android:exported="false"/>


        <activity
            android:name=".UserProfileActivity"
            android:exported="false">

        </activity>

        <activity
            android:name=".EditProfileActivity"
            android:exported="false">
        </activity>

        <activity
            android:name=".DevInfoActivity"
            android:exported="false">
        </activity>



    </application>

</manifest>